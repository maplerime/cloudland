- name: Set scrape interval variable
  set_fact:
    scrape_interval: 15  # 15 seconds

- name: Set prometheus_server_ip from monitor group
  set_fact:
    prometheus_server_ip: "{{ hostvars[groups['monitor'][0]]['ansible_host'] }}"

- name: Gather IP addresses from hyper group
  set_fact:
    hyper_ips: "{{ groups['hyper'] | map('extract', hostvars, 'ansible_host') | list }}"

- name: Validate WDS configuration
  fail:
    msg: "wds_address must be configured with a valid value when wds group exists"
  when:
    - "'wds' in groups"
    - "wds_address | default('') | length == 0 or wds_admin | default('') | length == 0 or wds_pass | default('') | length == 0"

- name: Extract WDS monitor server credentials
  set_fact:
    wds_monitor_server_ip: "{{ wds_address | regex_replace('^(?:https?://)?([0-9.]+)(?:.*)$', '\\1') }}"
    wds_monitor_server_admin: "{{ wds_admin }}"
    wds_monitor_server_pass: "{{ wds_pass }}"
  when:
    - "'wds' in groups"
    - "wds_address is defined and wds_address | length > 0"
    - "wds_admin is defined and wds_admin | length > 0"
    - "wds_pass is defined and wds_pass | length > 0"

- name: Generate node exporters config block
  set_fact:
    node_exporters_block: |
      - job_name: 'prometheus_node_exporter'
        static_configs:
          {% for ip in hyper_ips -%}
          - targets: ['{{ ip }}:9100']
          {% endfor %}

- name: Generate libvirt exporters config block
  set_fact:
    libvirt_exporters_block: |
      - job_name: 'prometheus_libvirt_exporter'
        static_configs:
          {% for ip in hyper_ips -%}
          - targets: ['{{ ip }}:9177']
          {% endfor %}

- name: Validate Alertmanager variables
  fail:
    msg: "Must define {{ item }} for task"
  when: vars[item] | default('') | length == 0
  with_items:
    - smtp_host
    - alert_sender
    - smtp_user
    - smtp_pass
    - receiver_name
    - alert_recipient
  tags: [monitor]

- name: Install packages for monitor server
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  apt:
    name: 
      - prometheus={{ prometheus_version | default("2.31.2") }}
      - prometheus-node-exporter={{ node_exporter_version | default("1.3.1") }}
      - prometheus-alertmanager={{ alertmanager_version | default("0.23.0") }}
    state: present
  tags: [monitor]

- name: Create Alertmanager system directories
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  file:
    path: "{{ item }}"
    state: directory
    owner: prometheus
    group: prometheus
  with_items:
    - /etc/alertmanager
    - /var/lib/alertmanager
  tags: [monitor]

- name: Configure and enable Alertmanager
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  block:
    - name: Configure Alertmanager
      copy:
        dest: /etc/alertmanager/alertmanager.yml
        content: |
          global:
            smtp_smarthost: '{{ smtp_host }}:587'
            smtp_from: '{{ alert_sender }}'
            smtp_auth_username: '{{ smtp_user }}'
            smtp_auth_password: '{{ smtp_pass }}'
            smtp_require_tls: true
          route:
            group_by: ['alertname']
            group_wait: 30s
            group_interval: 5m
            repeat_interval: 3h
            receiver: '{{ receiver_name }}'
          receivers:
          - name: '{{ receiver_name }}'
            email_configs:
            - to: '{{ alert_recipient }}'
              send_resolved: true
        owner: prometheus
        group: prometheus
        mode: 0640
    
    - name: Enable Alertmanager service
      systemd:
        name: alertmanager
        enabled: yes
        state: restarted
  tags: [monitor]

- name: Verify Alertmanager running status
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  uri:
    url: http://localhost:9093/-/healthy
    method: GET
    status_code: 200
    timeout: 5
  register: alertmanager_health
  until: alertmanager_health.status == 200
  retries: 6
  delay: 10
  tags: [monitor]

- name: Configure Prometheus server
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  copy:
    content: |
      global:
        scrape_interval: {{ scrape_interval }}s
      scrape_configs:
        - job_name: 'prometheus'
          static_configs:
            - targets: ['localhost:9090']
    dest: /etc/prometheus/prometheus.yml
    owner: prometheus
    group: prometheus
  tags: [monitor]

- name: Create alert rules directory structure
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  file:
    path: "{{ item }}"
    state: directory
    owner: prometheus
    group: prometheus
    mode: 0755
  with_items:
    - /etc/prometheus/rules_available
    - /etc/prometheus/rules_enabled
  tags: [monitor]

- name: Deploy CPU alert rules
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  template:
    src: files/cpu-alerts.yml.j2
    dest: /etc/prometheus/rules.d/cpu.yml
    owner: prometheus
    group: prometheus
    mode: 0640
  tags: [monitor]

- name: Deploy bandwidth alert rules
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  template:
    src: files/bw-alerts.yml.j2
    dest: /etc/prometheus/rules.d/bw.yml
    owner: prometheus
    group: prometheus
    mode: 0640
  tags: [monitor]

- name: Update Prometheus config to load enabled rules
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  replace:
    path: /etc/prometheus/prometheus.yml
    regexp: '^rule_files:'
    replace: |
      rule_files:
        - rules_enabled/*.yml
    backup: yes
  tags: [monitor]

- name: Update Prometheus with node exporters block
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  blockinfile:
    path: /etc/prometheus/prometheus.yml
    block: |
      {{ node_exporters_block | indent(2, True) }}
    marker: "# {mark} ANSIBLE MANAGED BLOCK - Node Exporters"
    insertafter: '^scrape_configs:'
    create: yes
  tags: [monitor]

- name: Update Prometheus with libvirt exporters block
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  blockinfile:
    path: /etc/prometheus/prometheus.yml
    block: |
      {{ libvirt_exporters_block | indent(2, True) }}
    marker: "# {mark} ANSIBLE MANAGED BLOCK - Libvirt Exporters"
    insertafter: '^scrape_configs:'
    create: yes
  tags: [monitor]


- name: Start Prometheus services on Prometheus server
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  systemd:
    name: "{{ item }}"
    enabled: yes
    daemon_reload: yes
    state: restarted
  loop:
    - 'prometheus-node-exporter'
    - 'prometheus'
  tags: [monitor]

- name: Add Grafana GPG key
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  apt_key:
    url: https://packages.grafana.com/gpg.key
    state: present
  tags: [monitor]

- name: Add Grafana repository
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  apt_repository:
    repo: deb https://packages.grafana.com/oss/deb stable main
    state: present
  tags: [monitor]

- name: Update apt cache and install Grafana
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  apt:
    update_cache: true
    name: grafana
    state: present
  tags: [monitor]

- name: Configure Grafana
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  copy:
    dest: /etc/grafana/grafana.ini
    content: |
      [server]
      http_port = 3000
      [database]
      type = sqlite3
      path = /var/lib/grafana/grafana.db

- name: Start and enable Grafana service
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  systemd:
    name: grafana-server
    state: started
    enabled: true
  tags: [monitor]

- name: Wait for Grafana to be ready
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  uri:
    url: http://localhost:3000/api/health
    method: GET
    status_code: 200
    timeout: 10
  register: grafana_health
  until: grafana_health.status == 200
  retries: 10
  delay: 5
  tags: [monitor]

- name: Add Prometheus as Grafana data source
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  uri:
    url: http://localhost:3000/api/datasources
    method: POST
    body:
      name: "Prometheus"
      type: "prometheus"
      url: "http://{{ prometheus_server_ip }}:9090"  
      access: "proxy"
      isDefault: true
    body_format: json
    status_code: 200
    headers:
      Content-Type: "application/json"
    user: admin
    password: admin
    force_basic_auth: yes
  ignore_errors: true
  changed_when: false
  tags: [monitor]

- name: Add monitor section to config.toml
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  blockinfile:
    path: /opt/cloudland/web/conf/config.toml
    block: |
      [monitor]
      host = "{{ prometheus_server_ip }}"
      port = 9090
    create: yes
  tags: [monitor]

- name: Add WDS monitor section to config.toml
  delegate_to: "{{ prometheus_server_ip }}"
  become: true
  blockinfile:
    path: /opt/cloudland/web/conf/config.toml
    block: |
      [WDS]
      host = "{{ wds_monitor_server_ip }}"
      port = 9090
      admin = "{{ wds_monitor_server_admin }}"
      password = "{{ wds_monitor_server_pass }}"
    marker: "# {mark} ANSIBLE MANAGED BLOCK - WDS"
    create: yes
  when:
    - "'wds' in groups"
    - "wds_address is defined"
  tags: [monitor]
